# 🚀 خطة العمل القادمة: تجهيز النظام للعملاء والتوزيع التجاري (Commercial Distribution & SaaS)

هذه الوثيقة تلخص خارطة الطريق للمرحلة القادمة لتحويل مشروع **Mobile-Store** إلى منتج تجاري جاهز للبيع والتثبيت عند العملاء بفترة تجريبية 20 يوماً أو باشتراك سنوي.

---

## 🏛️ 1. معمارية إدارة العملاء والتراخيص (Licensing & Multi-Tenancy)

### أ. عزل البيانات بين المتاجر (Store Isolation):
- كل عميل (صاحب محل) يمتلك حساباً رئيسياً مستقلاً (Tenant / Store).
- ضمان عزل كامل في قاعدة البيانات: لا يرى أي متجر مخزون أو مبيعات أو عملاء أو أرباح المتجر الآخر.

### ب. التحكم المركزي في التراخيص (Super Admin Control):
- لوحة إدارة مركزية تمكنك من:
  - إضافة متجر/عميل جديد وتحديد بيانات الدخول.
  - تحديد نوع الترخيص: `Trial` (فترة تجريبية 20 يوم) أو `Paid` (اشتراك سنوي/شهري).
  - تحديد تاريخ الانتهاء (`expires_at`).
  - تحديد الحد الأقصى للأجهزة المصرح بها (مثلاً: 1 كاشير ديسكتوب + 2 موبايل).
- بمجرد انتهاء المدة (20 يوماً)، يتم إيقاف الحساب تلقائياً ومطالبة العميل بالتجديد.

---

## 🔒 2. منظومة التحقق من العتاد لنسخة الديسكتوب (Hardware Verification)

لضمان عدم نسخ أو تشغيل البرنامج على أجهزة أخرى غير مرخصة:

1. **بصمة الجهاز (Machine Fingerprint / HWID):**
   - استخراج معرّف رقمي فريد ومحمي يعتمد على (Motherboard Serial + CPU ID + MAC Address).
2. **الربط التلقائي في أول تشغيل (Auto-Binding):**
   - عند أول تسجيل دخول للعميل، يقوم السيرفر السحابي بفحص الرخصة، وتسجيل بصمة هذا الجهاز كجهاز معتمد (`Authorized Device`).
3. **حماية النقل غير المصرح به:**
   - في حال تم نسخ البرنامج على فلاشة إلى جهاز آخر، سيرفض السيرفر تسجيل الدخول لاختلاف بصمة العتاد.
4. **توكن العمل بدون إنترنت (7-Day Offline Grace Token):**
   - إصدار توكن مشفر بمفتاح RSA صالح لمدة أسبوع للعمل في حالة انقطاع الإنترنت، مربوط بنفس بصمة الجهاز.

---

## 📦 3. حزم التوزيع للمنصات الثلاث (Distribution Packaging)

### 💻 أولاً: تطبيق الديسكتوب (Windows Installer)
- **تجميع الكود (PyInstaller):** تحويل البرنامج وملفات PyQt6 ومحركات التشفير إلى ملف تنفيذي مستقل (`.exe`) بدون الحاجة لتثبيت بايثون.
- **معالج التثبيت (Inno Setup / NSIS):** إنشاء ملف تثبيت احترافي `MobileStore-Setup.exe` يقوم بـ:
  - معالج تثبيت باللغة العربية والإنجليزية.
  - إنشاء اختصارات على سطح المكتب وقائمة Start.
  - دمج عنوان السيرفر السحابي `http://34.175.186.221/api` تلقائياً دون الحاجة لأي إعدادات يدوية من العميل.

### 📱 ثانياً: تطبيق الموبايل (Android Release APK)
- تجميع ملف `MobileStore.apk` بنسخة الإنتاج (Production Release).
- توفير رابط تحميل مباشر للـ APK من داخل لوحة تحكم الويب لإتاحة تنزيله بسهولة على هواتف العملاء وفريق العمل.

### 🌐 ثالثاً: منصة الويب (Web Dashboard)
- تعمل بالفعل على السيرفر السحابي `http://34.175.186.221`.
- دخول مباشر لكل عميل ببياناته الخاصة لمتابعة المتجر من أي مكان.

---

## 🗺️ 4. مراحل التنفيذ عند البدء (Execution Roadmap)

- [ ] **المرحلة 1:** تفعيل عزل المتاجر (Store Isolation) وإضافة ميزة الـ 20 يوم التجريبية تلقائياً في السيرفر.
- [ ] **المرحلة 2:** تجميع وبناء ملف تثبيت الديسكتوب `MobileStore-Setup.exe` واختباره على جهاز نظيف.
- [ ] **المرحلة 3:** استخراج ملف الـ APK النهائي وإتاحة رابط تنزيله للعملاء.
