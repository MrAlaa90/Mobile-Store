# دليل التطوير وخارطة الطريق الشاملة (Roadmap & Next Steps) - Mobile-Store

تم إنجاز وتثبيت كافة متطلبات الأمان التجاري، عزل البيانات، الهوية البصرية، وتطبيق الديسكتوب المحدث بنجاح.  
يوثق هذا الملف الإنجازات المكتملة بالتفصيل، والخطوات التشغيلية القادمة، مع خطة عمل تنفيذية متكاملة لإعداد وإطلاق **نسخة نظام iOS (iPhone & iPad)**.

---

## 📌 أولاً: سجل الإنجازات المكتملة رسمياً (Completed Milestones - v1.1.0 Enterprise)

### 1. عزل قواعد البيانات وتصفير بيانات المتاجر الجديدة (Tenant Isolation & Clean Database)
- **عزل قواعد البيانات المحلية على الديسكتوب**: تم فصل ملف SQLite لكل مستجر بحيث يحصل كل حساب على قاعدة بيانات مستقلة تماماً: `local_storage_{username}.db`، مما يمنع متجر A من رؤية بيانات متجر B على نفس الجهاز.
- **إزالة البيانات التجريبية الوهمية**: تم استئصال كافة أكواد حقن الهواتف والفواتير الوهمية من كود البرنامج، بحيث يفتح أي متجر جديد (مثل `store_alex`) واجهة نظيفة وفارغة 100% مطابقة تماماً لحسابه السحابي.
- **مزامنة المبيعات والمخزن السحابي (Two-Way Sync)**: إضافة سحب الفواتير والمخزن تلقائياً من السيرفر بمجرد تسجيل الدخول لضمان التوافق اللحظي بين الديسكتوب والويب.

### 2. نظام التحديث التلقائي من جهة المستخدم (In-App Auto-Updater)
- **فحص صامت ذكي**: فحص تلقائي صامت للتحديثات بعد فتح البرنامج بثوانٍ عبر نقطة النهاية السحابية `/api/app/check-update/`.
- **زر يدوي للتحديث**: زر `🚀 Updates` في الشريط العلوي وشاشة الدخول للتحقق في أي وقت بنقرة واحدة.
- **تحديث مدمج داخل البرنامج (One-Click Update)**: نافذة تظهر سجل التغييرات مع شريط تقدم مباشر لتحميل التحديث وتشغيل المثبت تلقائياً دون أي عناء لصاحب المحل.
- **أمان واستمرارية البيانات (Zero Data Loss)**: ضمان عدم مسح قواعد بيانات SQLite المحلية أو قاعدة بيانات السيرفر PostgreSQL أثناء أي تحديث للبرنامج.

### 3. ميزات الديسكتوب العصرية (Modern Desktop Features)
- **التحويل بين الوضع الداكن والفاتح**: زر `🌙 Dark / ☀️ Light` في الشريط العلوي لتطبيق الديسكتوب يغير مظهر كافة الجداول والنوافذ والأزرار فورياً ويحفظ الاختيار محلياً.
- **النسخ الاحتياطي الفوري بضغطة زر**: زر `💾 Backup` لتصدير بيانات المخزن والفواتير وأوامر الصيانة إلى ملف JSON منظم.
- **الدعم الفني المباشر عبر واتساب**: زر `💬 Support` يفتح محادثة واتساب مع رسالة تشخيصية تلقائية تحوي (اسم المتجر، معرف العتاد Hardware ID، رابط السيرفر المتصل، والمعاملات المعلقة).

### 4. الهوية البصرية والأيقونات ثلاثية الأبعاد (3D Neon Brand Identity)
- تصميم أيقونة ثلاثية الأبعاد 3D عالية الدقة وتطبيقها عبر المنصات:
  - **الديسكتوب**: كأيقونة رئيسية للبرنامج والمثبت وشريط مهام ويندوز (`app_icon.ico` و `app_icon.png`).
  - **الويب**: كـ `favicon.ico` وأيقونة شريط الملاحة `logo.png`.
  - **الموبايل**: أيقونة `icon.png` للتطبيق.

### 5. تأمين السيرفر وبوابة الأدمن السرية (Security & Obfuscation)
- نقل لوحة تحكم Django Admin إلى مسار سري مموه غير مدرج في قواميس SecLists: `ms-core-gate-7x9q/`.
- حجب مسار `/admin/` على مستوى Nginx وإرجاع `404 Not Found` لإفشال روبوتات الفحص والاختراق.
- إزالة كافة الروابط الموجهة للأدمن من الواجهة الأمامية للمتاجر.
- تصميم واجهة داكنة فخمة للوحة الأدمن متوافقة مع الهوية البصرية.

---

## 🚀 ثانياً: الخطوات القادمة الفورية (Immediate Next Steps)

- [ ] **1. تطبيق آخر تحديث على السيرفر السحابي (VPS)**:
  سحب التحديث الأخير الذي يتضمن عزل قواعد البيانات وملف التثبيت الجديد:
  ```bash
  cd ~/Mobile-Store
  git pull origin main
  sudo docker compose up -d --build
  ```
- [ ] **2. النسخ الاحتياطي السحابي التلقائي لقاعدة بيانات PostgreSQL (Automated Daily Backups)**:
  - إعداد مهمة Cron Job يومية على السيرفر لتصدير قاعدة البيانات `pg_dump` وحفظها في مجلد معزول أو سحابة خارجية (مثل Google Drive أو AWS S3).
- [ ] **3. ربط النطاق (Domain Name) وتفعيل شهادة الأمان المجانية (SSL / HTTPS)**:
  - ربط دومين خاص (مثال: `pos.mobilestore.com` أو `api.mobilestore.com`).
  - تفعيل Certbot / Let's Encrypt على Nginx لحماية كافة البيانات بتشفير HTTPS الإلزامي لمتاجر التطبيقات.
- [ ] **4. ربط بوابات الدفع الإلكتروني للاشتراكات (Payment Gateway)**:
  - ربط فوري أو Paymob أو Stripe للسماح لأصحاب المتاجر بتجديد اشتراكاتهم السنوية أو رخص أجهزتهم أونلاين.

---

## 🍏 ثالثاً: خارطة طريق إعداد وإطلاق نسخة نظام iOS (iPhone & iPad Roadmap)

نظراً لأن تطبيق الهاتف مبني بالفعل باستخدام إطار عمل **Flutter**، فإن الكود المصدري في مجلد `mobile/` مشترك بنسبة تتجاوز 95% بين Android و iOS. إليك خطوات وتجهيزات إطلاق نسخة الآيفون الرسمية:

### المرحلة 1: المتطلبات التأسيسية لحساب أبل والبيئة (Environment & Prerequisites)
1. **حساب مطور أبل (Apple Developer Account)**:
   - الاشتراك في برنامج مطوري أبل السنوي ($99/year) عبر [developer.apple.com](https://developer.apple.com).
   - إعداد اسم المؤسسة أو المطور الفردي وتفعيل المصادقة الثنائية.
2. **بيئة بناء نظام macOS (Build Machine)**:
   - يتطلب تجميع تطبيقات iOS جهاز بنظام macOS يعمل عليه **Xcode**، ويمكن توفيره عبر:
     - جهاز Mac محلي (MacBook أو Mac Mini).
     - أو استخدام خدمات البناء السحابي التلقائية (CI/CD) مثل **GitHub Actions (macOS runner)** أو **Codemagic**، مما يتيح التجميع السحابي دون الحاجة لشراء Mac فعلي.

### المرحلة 2: تهيئة كود Flutter لنظام iOS (Code & Project Configuration)
- [ ] **1. ضبط معرف الحزمة واسم التطبيق (Bundle Identifier & Display Name)**:
  - في ملف [mobile/ios/Runner/Info.plist](file:///d:/Projects/Mobile-Store/mobile/ios/Runner/Info.plist):
    - `CFBundleDisplayName`: "Mobile Store POS"
    - `CFBundleIdentifier`: `com.mobilestore.pos`
- [ ] **2. حزمة أيقونات iOS وشاشات البدء (AppIcon.appiconset & LaunchScreen)**:
  - توليد المقاسات المطلوبة لأيقونة التطبيق (20pt, 29pt, 40pt, 60pt, 76pt, 83.5pt, 1024pt) من الأيقونة الرسمية ثلاثية الأبعاد ووضعها في:
    `mobile/ios/Runner/Assets.xcassets/AppIcon.appiconset/`
- [ ] **3. أذونات الخصوصية (Privacy Permissions في Info.plist)**:
  - إضافة مبررات استخدام العتاد طبقاً لاشتراطات Apple Store:
    - `NSCameraUsageDescription`: "مطلوب إذن الكاميرا لمسح باركود الهواتف وقطع الغيار أثناء عمليات البيع والصيانة."
    - `NSPhotoLibraryUsageDescription`: "مطلوب لإرفاق صور الأجهزة التالفة وإثباتات الاستلام."
- [ ] **4. أمان الشبكة (App Transport Security - ATS)**:
  - تشترط أبل أن تكون كافة الاتصالات مشفرة عبر **HTTPS**.
  - فور تفعيل شهادة SSL على السيرفر، يتم توجيه الـ API إلى رابط آمن `https://yourdomain.com/api` بدون أي استثناءات لمنع رفض التطبيق أثناء المراجعة.

### المرحلة 3: التوقيع الرقمي والشهادات (Signing & Provisioning)
1. **إنشاء معرّف التطبيق (App ID)** في Apple Developer Portal.
2. **إنشاء شهادة التوزيع (Apple Distribution Certificate)**.
3. **إنشاء ملف التثبيت (Provisioning Profile)** من نوع *App Store Distribution*.
4. فتح المشروع في Xcode واختيار فريق التطوير وتفعيل *Automatically manage signing*.

### المرحلة 4: الاختبار التجريبي والإطلاق (TestFlight & App Store Release)
1. **بناء حزمة الإنتاج (Production Archive)**:
   ```bash
   cd mobile
   flutter build ipa --release
   ```
2. **الرفع إلى App Store Connect عبر Transporter أو Xcode**:
   - رفع ملف `.ipa` إلى حساب المتجر.
3. **الاختبار عبر TestFlight (Beta Testing)**:
   - دعوة أصحاب المتاجر والمختبرين لتجربة التطبيق مباشرة على أجهزة iPhone و iPad وإرسال الملاحظات.
4. **متطلبات صفحة المتجر والمراجعة النهائية**:
   - لقطات شاشة (Screenshots) لكافة مقاسات شاشات الآيفون والآيباد.
   - رابط سياسة الخصوصية (Privacy Policy URL) ودعم العملاء.
   - حساب تجريبي للمراجعة (Demo Credentials) لفريق مراجعة أبل (مثل `demo_store / DemoPass123`).
   - تقديم التطبيق للمراجعة (Submit for Review) والاعتماد الرسمي على App Store.

---

## 📋 جدول المتابعة السريعة (Quick Reference Status)

| المكوّن | الحالة الحالية | الخطوة القادمة |
|---|---|---|
| **الأمان والأدمن السري** | ✅ مكتمل ومحمي 100% | مراقبة السجلات |
| **الديسكتوب (Windows)** | ✅ مكتمل v1.1.0 ومجمّع | جاهز للتوزيع |
| **التحديث التلقائي للعميل** | ✅ مكتمل ومتصل بالـ API | تفعيل إشعارات الإصدارات |
| **عزل المتاجر والداتابيز** | ✅ مكتمل ومختبر | نسخ احتياطي دوري |
| **واجهة الويب (React)** | ✅ مكتملة ومتناسقة | ربط دومين و SSL |
| **تطبيق الأندرويد (APK)** | ✅ مكتمل ومتاح للتحميل | الرفع على Google Play |
| **تطبيق الآيفون (iOS)** | ⏳ جاهز للتجميع والتهيئة | إعداد Xcode وشهادات أبل |