# Django Backend (REST API)

This folder contains the unified Django REST API backend for the Mobile Store Management System.

## Architecture
- Django with Django REST Framework (DRF)
- Database: PostgreSQL (production/scalable) / SQLite (local dev)
- Token/License management
